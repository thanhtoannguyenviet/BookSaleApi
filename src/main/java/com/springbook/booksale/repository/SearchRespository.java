package com.springbook.booksale.repository;

import com.springbook.booksale.entity.AuthorEntity;
import org.springframework.data.jpa.repository.Query;

import java.util.List;

public interface SearchRespository {
//    @Query("select b from book b" +
//            "inner join author on b.id_author = a.id" +
//            "inner join topic on B.id_topic =  ")
//    public List<AuthorEntity> searchAuthor(String keyword);
//    @Query("select a from author a where a.name like %?1%")
//    public List<AuthorEntity> searchAuthor(String keyword);
//    @Query("select a from author a where a.name like %?1%")
//    public List<AuthorEntity> searchAuthor(String keyword);
}
